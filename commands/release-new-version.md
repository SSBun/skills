---
description: Release a new version — bump, commit, tag, push, optionally publish.
argument-hint: [version] [--skip-push]
---

Invoke the `release-new-version` skill to handle this request.

Arguments: $ARGUMENTS

The first positional argument (if any) is the target version (e.g. `1.2.3`).
Pass `--skip-push` to skip the remote push step.
